#/!/bash

git add .
if [ -z "$1" ]
then
    echo "No argument supplied"
    git commit -m "update"
else
    git commit -m "$1"
fi
commit -m arg
git push origin parsing # while parsing is the current branch =------------------------

